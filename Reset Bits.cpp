#include<bits/stdc++.h>
using namespace std;
int main()
{
  int n,k;
  cin>>n>>k;
  for(int i = 0;i<k;i++)
  {
    int bit_count = floor(log(n))+1;
    //condition if the ith bit is 1
    if(n  )
  }
  return 0;
}